{
  "name": "my-svelte-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "npx gh-pages -d build"

  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^2.0.0",
    "@sveltejs/kit": "^1.0.0",
    "gh-pages": "^4.0.0",
    "svelte": "^4.0.0",
    "vite": "^5.0.0"
  }
}
